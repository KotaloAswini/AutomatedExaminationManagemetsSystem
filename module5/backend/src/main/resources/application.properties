# Server Configuration
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/dutyexchange
spring.data.mongodb.database=dutyexchange

# Allow application to start even if MongoDB is not connected (for development)
spring.data.mongodb.auto-index-creation=false
